/#!/bin/bash pwd prints the absolute path of the executable.
/#!/bin/bash ls prints the contents of a directory.
/#!/bin/bash cd "$HOME" changes directory from pwd to the current user's directory.
